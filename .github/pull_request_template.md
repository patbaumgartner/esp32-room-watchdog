## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. -->

## How it was tested

- [ ] `./check.sh` / `./check.ps1` passes (unit tests, cppcheck, firmware build)
- [ ] Verified on hardware — describe what you observed:
- [ ] Behaviour change in `lib/` is covered by a new or updated test in `test/`
- [ ] Docs updated (`README.md`, `docs/`, `CHANGELOG.md`) if behaviour, wiring,
      or the HTTP API changed
